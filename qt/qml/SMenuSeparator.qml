import QtQuick
import QtQuick.Controls
import Seder.VideoRewrap

MenuSeparator {
    topPadding: Theme.s2
    bottomPadding: Theme.s2
    contentItem: Rectangle {
        implicitWidth: 150
        implicitHeight: 1
        color: Theme.border
    }
}
